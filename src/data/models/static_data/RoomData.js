export default class RoomData {
    constructor(roomName, roomId, roomPhotos, roomDescription, roomInfo) {
        this.roomName = roomName;
        this.roomId = roomId;
        this.roomPhotos = roomPhotos;
        this.roomDescription = roomDescription;
        this.roomInfo = roomInfo;
    }
}